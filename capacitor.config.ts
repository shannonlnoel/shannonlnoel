import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hiit.firstApper',
  appName: 'HIIT',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
